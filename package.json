{
  "name": "release-please-template",
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "packageManager": "pnpm@11.12.0",
  "description": "GitHub repository governance, CI, release, and coding-agent template.",
  "scripts": {
    "test": "node --test",
    "preinstall": "npx only-allow pnpm"
  },
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "lefthook": "^2.1.10"
  }
}
